# j-app-tracker frontend (React + TypeScript + Vite)

See the root README for end-to-end setup: `../README.md`.

## Run locally

```bash
cd fe
cp .env.example .env
npm install
npm run dev
```

## Environment variables

- `VITE_API_URL` — backend API base URL (default `http://localhost:4000/api`)
