export default function HomePage() {
  return (
    <main className="page">
      <section className="card" aria-label="App">
        <h1 className="title">Job Application Tracker</h1>
        <p className="muted">
          You are authenticated. Next step is building the Kanban board.
        </p>
      </section>
    </main>
  )
}
